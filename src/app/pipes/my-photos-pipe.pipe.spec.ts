import { MyPhotosPipePipe } from './my-photos-pipe.pipe';

describe('MyPhotosPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MyPhotosPipePipe();
    expect(pipe).toBeTruthy();
  });
});
